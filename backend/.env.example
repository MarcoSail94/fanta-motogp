NODE_ENV=development
PORT=3000

# Database
DATABASE_URL="postgresql://user:password@localhost:5432/fantamotogp"

# JWT
JWT_SECRET=your-super-secret-jwt-key
JWT_EXPIRE=7d

# Redis (per cache e sessioni)
REDIS_URL=redis://localhost:6379

# Firebase (per notifiche push)
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_PRIVATE_KEY=your-private-key
FIREBASE_CLIENT_EMAIL=your-client-email

# API Keys per dati MotoGP (se disponibili)
MOTOGP_API_KEY=your-api-key
SPORTRADAR_API_KEY=your-sportradar-api-key

# GitHub Actions dispatch (refresh dati race da web app admin)
# Token con permesso Actions/workflow dispatch sul repository
GITHUB_ACTIONS_TOKEN=github_pat_xxx
GITHUB_REPOSITORY=owner/repo
GITHUB_WORKFLOW_ID=cron-jobs.yml
GITHUB_WORKFLOW_REF=main

# Email Service (per inviti lega)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
